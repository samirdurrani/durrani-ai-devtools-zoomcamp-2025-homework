// Test setup file for Vitest and React Testing Library
import '@testing-library/jest-dom'

// Mock window.matchMedia which Monaco Editor uses
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: (query) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => {},
  }),
})
