# FastAPI Backend Requirements
# Install with: pip install -r requirements.txt

# Core FastAPI framework
fastapi==0.104.1
uvicorn[standard]==0.24.0  # ASGI server with WebSocket support

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# WebSocket support
websockets==12.0
python-multipart==0.0.6  # For form data parsing

# CORS support (included in FastAPI)
# Already included with fastapi

# Optional: For production deployment
gunicorn==21.2.0  # Production WSGI server
redis==5.0.1      # For session storage (optional)
docker==6.1.3     # For containerized code execution (optional)

# Optional: For code execution
# subprocess is built-in, but for better sandboxing:
# docker==6.1.3   # Already listed above
# pysandbox==1.6.0  # Python sandboxing (optional)

# Development tools
python-dotenv==1.0.0  # Environment variable management
httpx==0.25.2         # For testing HTTP endpoints
pytest==7.4.3         # Testing framework
pytest-asyncio==0.21.1  # Async test support
